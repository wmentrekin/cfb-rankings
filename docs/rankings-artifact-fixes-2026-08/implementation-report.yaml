artifact: implementation_report
feature: "rankings-artifact-fixes-2026-08"
mode: "bounded-feature"
status: complete
owner_skill: work
based_on: "docs/rankings-artifact-fixes-2026-08/plan.yaml"

summary:
  scope_executed: "B1/T1 (schema + guard) and B2/T2 (live cleanup + 2025 backfill), both complete."
  outcome: >
    artifacts/rankings.py now emits a standardized `conference` display string and a
    `logo` URL per ranking entry; main.py refuses to run (no DB write, no artifact
    publish) whenever the effective week is < 1, for both auto-computed and explicit
    --week invocations. The bad season=2026/week=0 batch (138 ratings rows, 3 R2 keys)
    was deleted and rankings/index.json self-healed back to 2025/week 15. All 2025
    weeks 1-15 were republished with the new fields.
    KNOWN GAP: the `logo` field is null across every republished 2025 artifact --
    season=2025's `teams.logos` rows are a legacy 2-URL ESPN-CDN format with no
    /logos-dark/32/ match, unlike season=2026's modern 16-URL CFBD-CDN format the
    requirements' research was scoped against. A live CFBD API check (this session)
    confirms the API now serves the modern format even when queried with year=2025,
    so this is fixable with a straightforward re-fetch-and-UPDATE of teams.logos for
    season=2025 -- but that's new scope beyond what requirements.yaml/plan.yaml
    covered (which explicitly listed the `teams` table as forbidden_scope for the
    cleanup task), so it wasn't done without checking with the user first.

task_batches_attempted: ["B1", "B2"]
task_batches_completed: ["B1", "B2"]

domains_touched: ["data-engineer"]

pr:
  number: 3
  url: "https://github.com/wmentrekin/cfb-rankings/pull/3"

changes:
  files:
    - "artifacts/rankings.py -- t.logos added to the ratings/teams join query, CONFERENCE_DISPLAY_NAMES dict + _display_conference() (maps 11 known raw values, warns+passes through unmapped ones), _resolve_logo() (substring-matches /logos-dark/32/ in the logos array, None on no match/empty/missing), both wired into build_payload()"
    - "main.py -- week<1 guard added immediately after args.week is finalized, before any DB/model/artifact work, covering both the auto-computed and explicit --week paths"

  modules: ["cfb-rankings"]
  docs_updated:
    - "docs/rankings-artifact-fixes-2026-08/status.yaml"

local_validation:
  checks_run:
    - "Synthetic build_payload() test: mapped conference + matching dark-32 logo, unmapped conference (passthrough + logged warning, no crash), team with empty logos list (logo: None) -- all 4 scenarios correct."
    - "uv run python main.py --year 2026 --week 0: guard fires, logs the reason, exits before any DB/model/publish work; verified via log inspection and a before/after DB row-count check (138 -> 138, unchanged)."
    - "uv run python main.py --year 2026 (no --week, auto-computed during current pre-season window): guard fires identically."
    - "main.py control-flow inspection confirms args.week=1 does NOT trip the guard (execution falls through unchanged)."
    - "Live production cleanup (real credentials, real mutation): DELETE FROM ratings WHERE season=2026 AND week=0 -- 138 rows before, 0 after, confirmed via direct query. teams table untouched, confirmed via row count."
    - "R2: rankings/2026/week-00.json, rankings/2026/latest.json, rankings/latest.json all confirmed present (head_object) before deletion and absent after. refresh_index() run; rankings/index.json confirmed latest_season=2025, latest_week=15 afterward."
    - "artifacts/backfill_rankings_artifacts.py run for 2025 weeks 1-15: all 15 published with zero warnings (no unmapped conference values encountered across the full season). Weeks 1/8/15 spot-checked from R2: all 136 entries have one of the 11 standardized conference strings; rank/team/record/rating/delta values internally consistent (records accumulate correctly, deltas populated, week-15 #1 team plausible)."
  failures: []
  skipped: []

review_summary:
  status: not_started
  key_findings: []

test_summary:
  status: not_started
  checks_run: []
  failures: []
  gaps: []

open_issues:
  - "BLOCKING for the paired personal-site feature's real-data goal (not for this feature's own code correctness): logo is null for all 2025 historical weeks due to a stale legacy-format teams.logos column for season=2025. A live CFBD API check confirms re-querying /teams/fbs?year=2025 right now returns the modern 16-URL format -- fixable via a straightforward teams.logos UPDATE for season=2025 rows, followed by re-running the 2025 backfill. Not yet done: touches the `teams` table, which requirements.yaml/plan.yaml explicitly scoped as forbidden for the cleanup task, so this is new scope pending a decision from the user before proceeding."
  - "2026 week 1 onward (first real run expected ~2026-09-06 per the new guard) will get real logos automatically once played, since season=2026's teams.logos is already in the modern format -- only the 2025 historical backfill is affected by the gap above."

handoff:
  current_state: "complete, pending one scope decision"
  next_owner: "user"
  next_action: "Decide whether to also refresh teams.logos for season=2025 (live UPDATE, new scope) so historical weeks get real logos too, or accept null logos for 2025's historical record and let logos start appearing from 2026 week 1 onward."
